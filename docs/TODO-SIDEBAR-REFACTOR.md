# 需求：左侧栏重构

> 优先级：P0（S1 内完成）
> 规范文档：`docs/UI-SIDEBAR-SPEC.md`

---

## 改动清单

### 1. icon-rail.tsx 重写

现有 3 个图标（项目/场景/设置）→ 改为 Home + 6 入口 + 设置：

```
[🏠] Home（项目切换下拉）
─────
[📦] 设备
[🏗️] 建筑
[🔗] 拓扑（灰色占位）
[🎬] 场景（灰色占位）
[📤] 分享（灰色占位）
[📋] 查看（灰色占位）
─────
[⚙️] 设置
```

S1 只做设备和建筑可点击，其余点击提示"开发中"。

### 2. Home 项目切换下拉

新建 `project-switcher.tsx`：
- 当前项目名称显示
- 下拉列表切换项目
- 底部：+ 新建项目 / 管理项目

### 3. 设备面板接入

选中"设备"图标 → 左侧面板显示 `DeviceCatalog`（已有）

### 4. 建筑面板

选中"建筑"图标 → 左侧面板显示 Pascal 原有的 `SitePanel` + `[结构][家具][区域]` Tab

### 5. 去掉"场景树/场景"顶部 Tab

用左侧图标栏替代，不再需要顶部 Tab 切换。
